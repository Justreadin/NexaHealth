// index.js - Homepage Specific Functionality

document.addEventListener('DOMContentLoaded', () => {
  // Any homepage-specific initialization
  // Most shared functionality is already in main.js
  
  // Example: Initialize any homepage-specific components
  const heroSection = document.querySelector('.hero-section');
  if (heroSection) {
    // Homepage-specific animation or interaction
  }
});